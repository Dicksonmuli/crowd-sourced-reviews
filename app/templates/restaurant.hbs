<div class="row">
  <div class="col-md-6" id="boxshadow">
   <img src={{restaurant.image}} alt={{restaurant.name}}>
  </div>
  <div class="col-md-6">
    <h3>{{restaurant.name}}</h3> <em>{{restaurant.city}}</em> in {{restaurant.type}}
    <label><b>Location:</b></label>{{restaurant.city}}
    {{restaurant.description}}
  </div>
</div>
<div class="">
	{{#each }}
		{{review-tile}}
	{{/each}}
</div>
  <br>
