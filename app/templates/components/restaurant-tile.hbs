<li>
  {{#link-to 'restaurant' restaurant.id}}
	<div class="col-md-6" id="boxshadow">
	  <span class="image">{{restaurant.image}}</span>
	</div>
  {{/link-to}}
  <div class="col-md-6">
    <h3>{{restaurant.name}}</h3> <em>{{restaurant.city}}</em> in {{restaurant.type}}
    <label><b>Location:</b></label>{{restaurant.city}}<br>
    {{#if isDescriptionShowing}}
      <p>Description</p>
      {{else}}
      <button{{action 'showDescription'}}>View details »</button>

    {{/if}}
  </div>
</li>
